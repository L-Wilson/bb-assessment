# Here we can provision shared infrastructure components used by multiple services.
# These are typically provisioned per environment (e.g., VPC, ECS cluster),
# with some exceptions depending on architecture (e.g., ACM/DNS).
